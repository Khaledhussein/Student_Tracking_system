#the main
###########################
import MySQLdb
import Instructors as ins
import students as std
import subjects as sub
############################
while x != "":
     # x is any P-key for the utilization i put the if statment
     # we input the P-key if the P-key doesnt equal NULL the loop will redo until its NULL
     #which mean there is no insertion process to do
     if x =="":
          break
     else:
          # input the rest to complete the insertion 
          
